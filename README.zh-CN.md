# chimivim

个人使用的 Neovim 配置文件。

## 软件依赖

- 始终推荐使用最新版本的 Neovim。本配置直接采用最新的原生 API，不考虑旧版本兼容。
- 启用外部插件时需要 Git。
- 推荐安装 `rg`、`fd` 和 `fzf`，用于文件与文本查找。
- Nerd Font 为可选依赖，推荐用于图标显示。
- 安装 Tree-sitter parser 时可能需要 C 编译器和 Tree-sitter CLI。
- LSP、格式化工具及其运行环境按所使用的语言安装，其中许多工具可以通过 Mason 管理。
